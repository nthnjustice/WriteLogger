<h1><%= current_user.name %></h1>
<div class="divider"></div>
<span><%= link_to "view my profile", current_user %></span>
<span>| (<%= pluralize(current_user.microposts.count, "writing log") %>)</span>